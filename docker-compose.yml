services:
  app:
    build:
      context: .
      dockerfile: Dockerfile 
    image: ccp-streamlit:latest
    ports:
      - "8000:8000"
    env_file:
      - .env.docker 
    restart: unless-stopped